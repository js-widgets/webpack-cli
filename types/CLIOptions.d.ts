export declare type CLIOptions = {
  outputDir: string;
  debug: boolean;
  existingRegistry?: URL;
  sourceDir: string;
  configFile: string;
  omitMissing: boolean;
  newVersion?: string;
  widgetDefinitions: string[];
};
